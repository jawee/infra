---
- hosts: 
  - proxmox-test
  roles:
    #- role: geerlingguy.docker
    #  become: true
    ##  vars: 
    #    docker_package: "{{ 'docker-ce' if ansible_os_family == 'Debian' else 'docker' }}"
    #    docker_users:
    #      - "{{ user }}"
    - docker
    #- sickchill
    #- couchpotato
    #- jenkins
    - tautulli
    - mount_remote_shares
